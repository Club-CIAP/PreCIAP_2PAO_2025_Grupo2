# conda env create --file environment.yml
name: housing_price_prediction
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.11
  - pip:
    - pyhere
    